# shopping-cart
A simple full-stack e-commerce web application using **Go (Gin + Gorm)** and **React** that supports user login, item listing, cart management, and order checkout with token-based authentication.
